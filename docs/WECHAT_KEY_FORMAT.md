# WeChat 4.x Key 格式（参考实现）

- 原始候选：64 个十六进制字符，对应 32 字节。
- 候选来源：配置 blob 经固定 XOR mask 解码后的 `x'...'` 文本。
- 结构假设：配置对象节点包含字符串指针/长度，随后对象含数据指针/长度。
- 验证：数据库第一页的盐、页尾 HMAC，使用 PBKDF2-HMAC-SHA512（2 次用于页 MAC）计算验证。

本文件不包含当前机器的 key、内存内容或密钥摘要。
