# 安全与合规

---

## 一、这个工具能做什么、不该做什么

### 适用场景

- 在**你自己的账号、你自己的设备**上做个人自动化
- 让 AI 按你的人设替你回复消息
- 归档导出你自己的聊天记录
- 学习 Windows UIA 自动化、SQLCipher 解密、进程注入等技术

### 明确不适用

| 场景 | 为什么 |
|---|---|
| 群发、营销、加粉 | 骚扰他人，违反微信服务条款，必然封号 |
| 对非本人账号操作 | 未授权访问他人数据 |
| 群聊自动回复 | 误发影响面大，且技术上不可靠 |
| 冒充他人身份 | 违法 |
| 收集他人聊天数据 | 侵犯隐私 |
| 任何对方不知情的场景 | 违背基本诚信 |

**开启自动回复意味着会以你的名义真实发出消息。** 请确保你的联系人不会因此被误导。

---

## 二、本工程内置的安全机制

| 机制 | 作用 |
|---|---|
| **只读操作前台保护** | 被动读取不得改变前台窗口；违反即失败关闭，防止静默干扰用户 |
| **发送结果验证** | 必须观察到输入框清空或消息落库，才算发送成功；不以"没报错"当成功 |
| **输出净化** | 拦截模型误输出的 `[图片]` `[文本]` 等占位符，避免发出无意义内容 |
| **本地 IPC 鉴权** | `127.0.0.1:18010` 需口令；默认值已被 `setup.py` 随机替换 |
| **单用户设计** | 只响应绑定账号，忽略其它来源的消息 |
| **只监听回环** | 全部服务绑定 `127.0.0.1`，不对外网开放 |
| **防死循环** | 连续回复上限 + 对面刷屏检测，避免两个机器人互刷 |
| **机主接管** | 你手动发言后该会话自动静默一段时间，交还控制权 |
| **决策留痕** | 每次回复的来信与回复写入 `logs/auto_reply.jsonl` |
| **发布前安检** | `tools/build_release.py` 扫描个人凭据，命中即拒绝打包 |

---

## 三、必须由你决定的开关

以下开关**默认关闭或保守**，开启前请确认你理解后果：

| 开关 | 默认 | 开启后的后果 |
|---|---|---|
| `auto_reply.enabled` | `false` | 开始回复白名单对象 |
| `auto_reply.dry_run` | `true` | 改为 false 才会真实发送 |
| `WECHAT_ENABLE_SEND` | 在服务配置里为 `1` | **本机任何进程都能通过 8010 真实发消息** |
| `WECHAT_ALLOW_KEY_EXTRACTION` | `1` | 允许密钥发现流程 |
| `human_override_minutes` | `15` | 设为 0 则你手动发言不再暂停自动回复 |

**`WECHAT_ENABLE_SEND=1` 是最需要留意的一项**：它意味着只要本机能访问
`127.0.0.1:8010`，就能以你的名义发微信。单用户机器上通常可接受，
但如果你机器上有不受信任的进程，请改为 `0`。

---

## 四、密钥与数据的安全

### 敏感文件清单

| 文件 | 内容 | 处理 |
|---|---|---|
| `work/db.key` | 数据库密钥（明文） | 已在 `.gitignore`；**绝不能提交或分享** |
| `work/scrub_rules.json` | 真实姓名 / wxid / 主机名脱敏字典 | 同上；构建时自动排除，见 SECRETS.md 第六节 |
| `work/database_key_cache.json` | 密钥缓存 | 同上 |
| `work/decrypted_db/` | **解密后的全部聊天记录** | 同上，最敏感 |
| `config/agent.env` | 本机 IPC 口令 | 同上 |
| `config/auto_reply.json` | API Key + 好友 wxid + 记忆 | 同上 |
| `config/console.json` | API Key | 同上 |
| `config/backups/` | 上述配置的历史副本 | 同上 |
| `logs/auto_reply.jsonl` | 聊天内容片段 | 同上 |
| `hook/build/*.log` | 含 wxid 的调试日志 | 发布时应排除 |

### 保护措施

```powershell
# 1) 确认 .gitignore 覆盖
Get-Content .gitignore | Select-String "work|config|logs"

# 2) 分享代码前先跑脱敏校验
python tools/build_release.py --check

# 3) 限制解密目录权限（可选）
icacls work\decrypted_db /inheritance:r /grant:r "$env:USERNAME:(OI)(CI)F"
```

### 聊天记录的敏感度

`work/decrypted_db/` 里是你**全部聊天历史的明文**。它等同于：
- 你的社交关系图
- 所有对话内容（含图片、转账、位置）
- 联系人备注与微信号

**备份、同步、上传任何云盘前请三思。** 建议只留在本机，或加密后存放。

---

## 五、账号风险

### 微信官方立场

微信服务条款**不鼓励**第三方自动化客户端。使用本工具存在被风控的风险，
包括但不限于：功能限制、临时封禁、永久封禁。

### 降低风险的建议

```
✅ 单账号每小时回复控制在 60 条以内
✅ 保留随机延迟（min_delay_seconds / max_delay_seconds）
✅ 设置静默时段，不要在深夜活跃
✅ 不主动群发、不加陌生好友
✅ 不要用主力账号承接高频业务
❌ 不要短时间大量发送相同内容
❌ 不要在多台设备同时登录同一账号跑自动化
```

### 更稳妥的替代

如果目的是**商业客服**，请使用：
- 企业微信 + 官方 API
- 微信公众号 + 官方客服接口

个人号自动化不适合承接商业流量，投入产出比和风险都不划算。

---

## 六、技术层面的风险提示

| 风险 | 说明 |
|---|---|
| **hook DLL 无法卸载** | 一旦注入微信进程，只能通过重启微信清除；改协议时新旧 DLL 会混流 |
| **进程注入** | `wxwal10.dll` 通过 IAT hook 注入微信；杀软可能报毒。介意者请勿使用 |
| **内存读取** | 取钥工具需要读取微信进程内存（本工程自身不做，但取钥工具通常需要） |
| **本地明文密钥** | `work/db.key` 是明文；能读到该文件的进程即可解密你的聊天记录 |
| **前台抢占** | 切换会话时会短暂前置微信窗口（已做到点完立刻还原） |
| **模型误答** | LLM 可能编造事实；对涉及金额、承诺、时间的内容请加硬约束话术 |

---

## 七、发布前检查清单

把本工程分享给他人之前，逐条确认：

- [ ] `work/db.key` 不在包里
- [ ] `work/decrypted_db/` 不在包里
- [ ] `config/*.json`（非 example）不在包里
- [ ] `config/agent.env`、`config/backups/` 不在包里
- [ ] `logs/` 不在包里
- [ ] `hook/build/*.log`、`*.pdb` 不在包里
- [ ] 代码里没有本机绝对路径（`C:\Users\<你的名字>`）
- [ ] 代码与文档里没有你的 wxid、好友 wxid、主机名
- [ ] 没有真实 API Key（搜 `sk-`）
- [ ] `python tools/build_release.py --check` 通过

最后一项是自动化的：它内置敏感模式扫描，**命中任何一项就拒绝产出压缩包**。

---

## 八、法律与道德

- 使用前请确认符合你所在地区的法律法规
- 只在你拥有合法访问权的账号与设备上使用
- 尊重聊天对象的隐私：自动回复应在对方可合理预期的范围内
- 不要用技术能力做伤害他人的事

**这个工具的能力边界由使用者决定。** 请让它停在该停的地方。
